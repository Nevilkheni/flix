/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['netflix-images.com', 'occ-0-2480-116.1.nflxso.net'],
    },
};

export default nextConfig;

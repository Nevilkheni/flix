import React from 'react';
import '@/app/globals.css'; 

const Loading = () => {
    return (
        <section>
            <div className="spinner"></div>
        </section>
    );
};

export default Loading;
